// 2.3 The modern mode, 'use strict'

//"Use Strict"; Defines that Javascript code should be executed in "strict mode".

// Declearing strict mode 
/*  Strict mode is declared by adding "use strict"; to the beginning of a script or a function. */

/*
"use strict"

function Sum(a,b)
{
   var add = a+b;
    alert(add);
}

Sum(12,3);       //code is invalid strict mode dosnt support add. replace var add instead.
*/

"use strict"

function Sum(a,b)
{
   var add = a+b;
    alert(add);
}

Sum(12,3);